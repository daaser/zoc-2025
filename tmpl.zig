const std = @import("std");
const input = @embedFile("input.txt");

pub const Day = struct {
    pub fn partOne() !u64 {
        return 100000;
    }

    pub fn partTwo() !u64 {
        return 0;
    }
};
